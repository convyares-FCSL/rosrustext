# Lifecycle Parity – ros2rust Adapter (placeholder)

This document will track lifecycle parity for the ros2rust adapter once it is
implemented. Mirror the structure of:
- `docs/adapters/roslibrust/lifecycle/parity.md`
